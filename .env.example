# Copy to .env for local deployment helpers, or set these in your cloud host.
# main.py reads environment variables directly; it does not require python-dotenv.

OPENAI_API_KEY=
STOCKLENS_DB_PATH=./data/stocklens.sqlite3
HOST=127.0.0.1
PORT=8000
STOCKLENS_COOKIE_SECURE=0

